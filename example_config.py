"""This config file should be moved to ./smsanalyzer/config.py
The example keys listed below should be the complete set of recognized keys.
If it seems I'm behind on the listing, please raise an issue or send
a pull request"""

config = {
    'dbpath': 'path to /data/data/com.textra/databases/messaging.db file'
}
